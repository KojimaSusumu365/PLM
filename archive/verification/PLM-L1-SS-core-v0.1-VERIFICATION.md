# PLM-L1-SS-core v0.1 配布後検証

ZIP作成後に実際に展開し、その展開物から検証プログラムを実行した。検証は成功した。ただし、実験の全合格を意味しない。外部教師受信1件の同期保留により、実験の厳格な全入力成功判定は `passed=false` のまま保持している。

## 配布物

- ZIP: `PLM-L1-SS-core-v0.1.zip`
- サイズ: 4,546,026 byte
- 収録: 233ファイル（manifest自身を含む）
- SHA-256: `4907b054343aeaad4fbfa000a41afa311c9f58a1fdeaa10da800597076a31f1d`
- source/model/data凍結SHA-256: `1e2a7a6dbdb4395926816039fd73cefe6a3e41f59870cdcf060679a63e0001cd`

## 実施した確認

- ZIP CRC検査と全233ファイルの展開後バイト一致。
- 展開物から新規30テストを再実行して成功。
- 全12組の保存記憶をロード。
- 代表12照会と、1組の全教師系列を再実行して一致。
- 全36受信の数値入力・受信結果を再生成して一致。
- 完走した全評価の反復結果56ファイルが完全一致（時間記録を除く）。
- 教師データ・専用学習モジュール・評価コードのない隔離環境から、別プロセスで2例の生成に成功。
- manifest対象232ファイルを検査。ZIPハッシュは検証後も不変。
- 元Bridgeの1,718ファイル、元ZIP、継承コピー103ファイルを変更していないことも確認。

## 結果の読み方

逐次方式の主評価は108/108照会で意味保持生成に成功し、216出力を生成した。一括方式も同じ成績で、精度の優位は示していない。

異常読出し・書込みに対する21件の更新保留は全て元の記憶を維持した。一方、正常パイロット下の本文雑音による更新は3/3で通過し、この条件の更新後生成精度は未評価である。

実装はPython離散時間シミュレーション。物理回路、汎用言語生成、全層SS化を達成したという意味ではない。詳細はZIP内の `REPORT.md` と `SPECIFICATION.md`。

機械可読の検証記録は隣接する `PLM-L1-SS-core-v0.1-POSTZIP.json`。この文書とPOSTZIP記録は、ZIP自体のハッシュを含む最終検証なのでZIPの外に置く。
