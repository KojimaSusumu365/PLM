# 凍結前の設計・修正記録

2026-09-07。以下は開発用データによる点検であり、固定した正式評価の結果ではない。

1. 時間記憶は、既知の六種類の構造キーが小さいため、部分特徴削減を使わず完全関係キーを照会する設計にした。事象内容・文内語順は設計上キーに含めない。未学習の時間方向に対して「同じmarkerを出せばよい」と一般化させず、未知の構造キーは保留する。元の一事象部品のID3型選択は変更していない。
2. NaN/Infを含む不正パケットは保留されていたが、複素数の組立て前に有限性を検査することで、不要なNumPy警告を出さず拒否するようにした。意味演算の式や閾値は変更していない。
3. 初回開発診断の雑音RNGはseed文字列を整数化して32bitに切っていたため、同じ先頭四文字のseedで同じ雑音方向になっていた。正式評価前にSHA256由来の64bit seedへ修正した。開発と正式評価の雑音seedも別名へ明示分離した。初回の測定コードを`MEASUREMENTS_DEV01.py`、初回の開発結果は作業証跡として保持する。初回の雑音二seedを独立した二方向の検証とは解釈しない。

最終の学習・データ・方式・閾値・評価条件は正式評価前にSOURCE_MANIFEST.jsonで固定する。凍結前の開発結果を未知評価として数えず、正式評価を見て合わせ込んだ変更とも混同しない。
