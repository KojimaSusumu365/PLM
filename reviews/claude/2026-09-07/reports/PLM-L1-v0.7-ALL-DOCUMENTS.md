# PLM-L1-v0.7 全Markdown資料（vendor重複除外）

原文を変更せず閲覧用に転記。原文中の相対パスは各資料の元ディレクトリを基準に読みます。過去版の将来計画は当時の記録であり、最新の実装状態や今回の作業指示ではありません。

## 原本 `README.md`

保管先: `PLM-L1-v0.7/README.md`  
SHA256: `4e57d00c83cef94920213e924dde74e8abcce20f69cddf683d7eb679a058ead5`

````markdown
# PLM-L1 v0.7：二事象の分離表現と意味保持生成

明示的に区切られた二文を読み、二つの出来事の主語・目的語・述語・肯否・仮定を一つのSS/VSA型位相信号に結合する。生成側はその数値信号から二事象を取り出し、それぞれの語順を変えて書く。原文を生成器に渡さない。

今回のv0.7は二事象の最小実証。先に提案したv0.6の負荷偏り対策とは別の作業であり、その弱点を解決した版ではない。

## 起動例

Python 3.12 / NumPy 2.3.5を想定し、Python本体は同梱しない。必要な場合だけ `python -m pip install -r requirements.txt`。このREADMEのフォルダで実行する。

```powershell
$env:OPENBLAS_NUM_THREADS='1'
$env:PYTHONIOENCODING='utf-8'
python -B -m plm_l1_v07 read --model results/model --text '太郎が花子を助けた。花子が太郎を助けなかった。' --out work/packet.json
python -B -m plm_l1_v07 recover --model results/model --packet work/packet.json
python -B -m plm_l1_v07 generate --model results/model --packet work/packet.json --goals object subject
```

意味が正しく保持される場合の出力は「花子を太郎が助けた。花子が太郎を助けなかった。」。二事象の順は保持し、第一文だけ目的語先行に変える。`--goals` の二項は、それぞれの事象の文内語順であり、時間順や因果関係を指定するものではない。

```powershell
python -B -m plm_l1_v07 encode --model results/model --meaning examples/MEANING.json --out work/direct-packet.json
python -B -m plm_l1_v07 generate --model results/model --packet work/direct-packet.json --goals subject object
python -B -m plm_l1_v07 train --pairs data/folds/object_negative/train.json --lexicon data/lexicon.json --component-seed banked-evaluation-0 --event-seed events-evaluation-0 --out work/refit-model
python -B -m unittest discover -s tests -v
python -B evaluate.py --out work/evaluation-rerun
python -B verify_release.py --out work/verification-rerun
```

出力は上書きしない。再実行では新しい出力名を使う。保留は終了コード2。第二文が読めない場合も、部分的な意味パケットや第一文だけの生成を成功として出力しない。`work/` は配布整合性検証の対象外で、作業出力に使える。

## 内容と制約

- 一事象の読み方・書き方は、v0.6と同じ432文／意味対から学習する。v0.6の部品コードを変更せず同梱し、主モデルの部品重みも一致させる。
- 二事象の区切り・位置符号・結合規約は設計済みで、学習により獲得したものではない。二事象の教師例は訓練に使わない。
- 標準方式 `bound` は同じD係数へ二事象を重ねる。`partitioned` は前半／後半に分ける対照、`unbound` は事象符号を外す診断用。後者は順序情報を失うため通常利用には適さない。
- 数値パケットは一組のreal/imag配列だけ。原文・二事象の意味一覧・二つの下位パケット・正解候補の絞り込みを添付しない。`recover` は確認用に復元した意味を表示するが、その表示を生成器へ転送する経路ではない。
- 二文を句点「。」で明示的に区切る。代名詞、省略、未知語、三つ以上の出来事、自由長文は対象外。提示順は扱うが、時間・因果・矛盾の解決は行わない。
- 既知部品の新しい二文組合せを制御文法で評価する。新しい自然文文法の学習や、独立した人手コーパスへの一般性を示すものではない。
- 基底生成・結合・重畳・相関は位相演算だが、特徴学習・分割・制御・文字列組立には通常Pythonが残る。全SS学習、P1観測マスク/S1チップ列・同期の接続、R1推論開放、Concept更新は行わない。

仕様は `SPECIFICATION.md`、結果は `results/REPORT.md` と `results/RELEASE_DECISION.md`、各試行は `results/EVALUATION.json`、検証は `verification/`。`vendor/PLM-L1-v0.6` は旧版回帰・独立採点用で、新しいランタイムはvendorを参照しない。
````

## 原本 `results/RELEASE_DECISION.md`

保管先: `PLM-L1-v0.7/results/RELEASE_DECISION.md`  
SHA256: `8bd24a2de5159e5afb590242b8698b975892b1be214cc225cf2dd8c1e5ae4af3`

````markdown
# PLM-L1 v0.7：実施結果と次の判断

2026-09-07

## 達成したこと

明示的に区切った二文について、各事象の主語・目的語・述語・肯否・仮定を一つの位相信号に結び付け、その数値信号だけから二文を生成する最小実証を実装した。生成器へ原文・二事象の意味一覧・下位パケット一覧を渡さない。提示順と各文の意味を保持したまま、文内語順をそれぞれ指定できる。

|標準bound・四事象seedの合計|完全一致|
|---|---:|
|読解・二事象の意味と提示順|1152/1152|
|直接生成・意味と指定語順|1152/1152|
|読解→生成・意味と指定語順|4608/4608|
|固定受入条件|364/364|
|新版＋旧版の機能／回帰テスト|381/381|

九カテゴリ（別々の人物・共通主語・共通目的語・役割連鎖・主客逆転・肯否差・仮定差・同じ事象の二回提示・述語差）を別々に採点した。各文のスロット正解だけでなく、両事象と提示順がすべて正しいときに完全一致と数えた。

例：『太郎が花子を助けた。花子が太郎を助けなかった。』を読み、第一文だけ目的語先行に指定すると『花子を太郎が助けた。花子が太郎を助けなかった。』を生成する。否定が第一事象へ移ったり、第二事象の主客が第一事象に置き換わったりしない。

## SS型結合の寄与と対照

事象符号を外して重ねるunbound対照では、異なる二事象の順序を交換しても完全に同じ数値信号になる。標準boundと前半／後半へ分けるpartitionedでは異なる信号になる。この差は単なる文の連結ではなく、事象の帰属・提示位置を保持する符号が必要であることを示す。

同じD個の複素係数を使うpartitioned対照も標準条件で成功した。したがって重畳型SSが唯一の解法・性能上優越という結果ではない。予算を揃えたのは数値ペイロードの係数数だけで、基底配列・全RAM・速度の同一条件比較ではない。低次元や雑音の結果と失敗はREPORT.mdに全条件を掲載した。

## 学習・評価・独立性

一事象の部品はv0.6と同じ432対から再学習し、旧主モデルと同じfingerprint・重みを確認した。新しく学んだのは二事象の区切り方や結合規則ではない。それらは今回設計した。訓練には二事象例を渡していない。四つのseedは新しい事象符号だけを変えており、四つの独立した部品学習条件ではない。

開発／評価はそれぞれ72の順序付き意味対×4入力語順=288文書で、意味対の重複はない。既存の語彙・制御文法・一事象分割を基に新しい二文組合せを作った実証であり、独立収集した自然文コーパスではない。評価の第一事象の主語／目的語／述語の組は部品訓練に含まれないが、第二事象は既知の組になる場合がある。

旧教師・評価データ・二事象コーパスのない別環境で同じモデルを再学習できた。新旧両方のreader.py/training.pyを外した生成専用環境でも、一つの数値パケットだけを転送して四例の生成に成功した。ただし共有の学習済み部品重みと補助コードは残る。第二文が失敗した場合は第一文だけを成功として出さず、全体を保留する。

固定ソース377ファイルと評価条件で全数値評価を2回実行し、結果JSON・報告・モデル情報・部品重み配列が完全一致した。381件は1回あたりのテスト数である。旧L1/P1/S1の1759ファイルと同梱v0.6の337ファイルを保持した。

## 未達と次の焦点

今回扱う順序は文章上の提示順だけで、現実の時系列・原因結果・矛盾解消ではない。代名詞、主語省略、接続表現の意味、三事象以上、自由長文は扱わない。v0.6の負荷偏り対策は今回見送り、解決したとは主張しない。

次の候補は、内容が同じ二回の提示も区別したまま、二事象の関係を限定して表現する段階。ただしまずは明示的な関係ラベルを持つ教師対で範囲を固定し、時間順と提示順を混同しない評価が必要になる。自動的に因果推論を開放する段階ではない。

通常Pythonの分割・特徴学習・制御は残る。全SS学習、P1の部分観測、S1チップ列／同期との統合、R1推論開放、Concept更新は未実装。

ソース固定digest：`321a049d5e38d4b6103c6666baf8a3084319a41087902b1823a64f0a4c697dc8`

結果digest：`cd2169e4efa51bfd207e8688a9dd16a86c424fe6340ae2e2ed63069d05c6be3c`

モデルfingerprint：`a9a901bebbfd378fd8d782da5b8a5216aca0759febdf759f53f301cc19a9f0b0`
````

## 原本 `results/REPORT.md`

保管先: `PLM-L1-v0.7/results/REPORT.md`  
SHA256: `b3833cc829b58ea259e5d547407090e35e283f4f423af149ab8ead8b6fd45521`

````markdown
# PLM-L1 v0.7 評価結果

## 二事象の読解・生成

|方式|読解・両事象完全一致|直接生成・意味と語順一致|往復・意味と語順一致|
|---|---:|---:|---:|
|bound|1152/1152|1152/1152|4608/4608|
|partitioned|1152/1152|1152/1152|4608/4608|

一文ごとの正解ではなく、二つの出来事の五スロットと提示順を両方保持した場合だけ完全一致と数える。生成はさらに各文の指定語順を満たす必要がある。

## 共有信号の低次元・符号なし対照

|D|方式|復元正答|受理誤答|保留|異なる二事象の順番交換で信号が同じ|
|---:|---|---:|---:|---:|---:|
|128|bound|213/288|0|75|0/256|
|128|partitioned|232/288|0|56|0/256|
|128|unbound|32/288|0|256|256/256|
|512|bound|287/288|0|1|0/256|
|512|partitioned|287/288|0|1|0/256|
|512|unbound|32/288|0|256|256/256|
|2048|bound|288/288|0|0|0/256|
|2048|partitioned|288/288|0|0|0/256|
|2048|unbound|32/288|0|256|256/256|
|8192|bound|288/288|0|0|0/256|
|8192|partitioned|288/288|0|0|0/256|
|8192|unbound|32/288|0|256|256/256|

unboundは事象の符号だけを外した対照。異なる出来事の順序が消える。partitionedは前半・後半を事象別に割り当てる対照で、同じD係数を持つ。比較予算は数値パケットの係数数だけであり、実行時の基底配列・全RAM・計算時間は一致させていない。

## 共有信号への数値雑音（bound）

|D|相対L2雑音|復元正答|受理誤答|保留|
|---:|---:|---:|---:|---:|
|128|0|67/144|0|77|
|128|0.05|66/144|0|78|
|128|0.15|62/144|0|82|
|128|0.3|0/144|0|144|
|128|0.6|0/144|0|144|
|8192|0|144/144|0|0|
|8192|0.05|144/144|0|0|
|8192|0.15|144/144|0|0|
|8192|0.3|0/144|0|144|
|8192|0.6|0/144|0|144|

雑音試験は評価者の意味から直接作った数値信号の復元試験で、読解→通信→生成の実証ではない。P1観測マスクやS1チップ列・同期は統合していない。

## 検証の範囲

固定受入：364/364。開発実行には合否判定を付けない。

一事象の読解・生成部品はv0.6と同じ432対・同じseedで学習する。新しい事象符号のseedだけを変える。二事象の区切りと位置符号は設計したもので、二文対から学習したものではない。訓練APIに二事象例や評価正解は渡していない。

二文は明示的な句点で分割する。提示順と各事象の役割・否定・仮定を扱い、時系列・因果・代名詞・省略・矛盾解消・自由長文は扱わない。生成器へ原文や意味のJSON一覧を転送せず、一つの数値パケットと二つの出力語順指定だけを渡す。意味スロットの復元後には通常Pythonによる制御処理が残る。

新しい二文の組合せを既存の制御文法と語彙から構築した試験で、独立に収集した自然文の評価ではない。partitioned対照も成功する場合、共有SS符号化が唯一の方法・性能上優越すると主張しない。v0.6の記憶負荷偏り問題を解消した実験でもない。
````

## 原本 `SPECIFICATION.md`

保管先: `PLM-L1-v0.7/SPECIFICATION.md`  
SHA256: `3438cadaa2b2f495a1854b2020cd5b8a2a759b4045d5f1c6f227f3dddc18d712`

````markdown
# 二事象信号の仕様

## 結合と復元

事象iの枠は `F_i = P + Σ_r R_r * V(value_i,r)`。Pは存在信号、Rは五つの意味役割、Vは候補値の単位複素位相符号。事象の提示位置0/1に別の符号B_iを割り当て、標準信号を `z = (B_0*F_0 + B_1*F_1)/sqrt(2)` とする。存在信号を含めて十二成分を一つのD次元信号に重ねる。保存する二事象一覧や全文対応表ではない。

復元は各事象のBと役割Rを使う相関照合で行う。コードでは `B_i*R_r*V` を前計算した基底との相関を使い、B_iの共役で取り出してR/Vと照合する式と同値になる。元信号の1/sqrt(2)を相関時に補正する。存在相関0.65以上、候補首位0.65以上、次位またはゼロとの差0.25以上を要求する。両事象を再符号化した信号との相対L2残差が0.20以下でなければ保留する。閾値は評価前に固定する。

一方の主語が他方の目的語であっても、事象位置と文内役割を別々に結合する。二つの内容が完全に同一でも、二回の提示を二つの位置に保持し、重複除去しない。否定や仮定は各事象の独立したスロットであり、二文全体を一括して否定・仮定にしない。

この照合は受信内容の内部整合性の検査で、元の文章に対する意味的真実や改竄防止の証明ではない。別の有効な意味へ再符号化された信号は、別の意味として受理される。意味正答は評価専用の独立文法で採点する。

## 読解・生成の境界

読解：二文を明示的な句点で分割し、v0.6読解器でそれぞれを読む。一事象の信号から得た内部候補を新しい共有信号へ結合し、両方の候補に復元できることを検査してから受理する。外部の評価正解はこの検査に与えない。

生成：共有信号から両事象を復元する。その結果をv0.6の一事象信号へ内部で再符号化し、学習済み部品生成器で二文を作る。原文・読解トレース・評価正解を入力しない。二つ目の生成が失敗した場合も、第一文だけを成功として出さず全体を保留する。

転送schemaは `plm-two-event-signal-v1`。許可するフィールドはschema/model_fingerprint/dimension/real/imag/eligible_for_inferenceのみ。Dは128～16384の128倍数。event_count=2はモデル仕様に固定し、照会ごとのメタデータで意味や境界を運ばない。旧版や別mode/seed/modelのパケットを拒否する。

モデルは新しい事象コードのseed/D/mode、提示順のみという解釈、固定受理規約、およびv0.6部品モデルのfingerprintから識別する。部品モデルは併せて保存する。モデルhashは取り違えや偶発的変更の検査で、暗号署名ではない。すべての出力で `eligible_for_inference=false`。

## 学習と既存版の扱い

部品は旧版と同じ単一事象432対・初期語彙、8192次元、banked-evaluation-0、split_proofで学習し直す。二事象の例、評価語順ラベル、操作列は学習入力ではない。四つの評価seedで変えるのは新しい事象コードだけで、部品重みは固定する。

二事象の分割規約・符号結合・生成順の制御は今回の手設計。二文から構造を学ぶ段階、代名詞解決、因果推論、複数文計画まで到達したと表現しない。v0.6のバンク負荷集中の弱点と、通常Pythonによる依存特徴選択は変えていない。

## 対照・評価

開発／評価それぞれ72の順序付き意味対、四つの入力文内語順で288文書を用意する。九カテゴリは、別々の登場人物、共通主語、共通目的語、役割連鎖、主客逆転、肯否の違い、仮定の違い、同じ事象の二回提示、述語の違い。開発／評価の順序付き意味対は重ならない。評価の第一事象の主語・目的語・述語の組は部品訓練にない。第二事象は変換によって既知の組になる場合もあり、両事象の全要素がすべて新規とは主張しない。

通常評価は二方式×四事象seed。各条件で読解288、直接生成72意味対×4語順=288、往復288入力×4語順=1152を試す。独立採点器で、両事象の五スロット・提示順・各文の出力語順を確認する。評価器は旧版の制御文法を使い、新読解器による自己採点ではない。新しい組合せの人工文書であり、新規自然文コーパスとは区別する。

`partitioned` は二つのD/2幅に枠を別々に置き、総係数数Dを揃える。`unbound` はB_iを外すので、異なる事象の順序を交換しても完全に同じベクトルになる。この情報消失を直接比較する。partitionedでも成功した場合、それはSS重畳だけの唯一性・優越性を示さない。相関基底を含む全ランタイムのRAM/速度は一致させておらず、予算一致は数値ペイロードの係数数だけ。

別試験でD=128/512/2048/8192と数値雑音を測る。低次元での保留・誤復元も記録し、標準条件の成功に混ぜない。雑音は評価意味から直接生成した共有信号に加える相対L2摂動で、S1通信路やP1観測マスクの実装ではない。時系列、原因結果、代名詞、省略、自由長文への接続は次段階。
````

