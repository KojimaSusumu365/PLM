# PLM-L1 v0.13 配布検証

実施日：2026-09-08。結果：合格。

## 配布物

- `PLM-L1-v0.13.zip`：48,928,008 bytes、84ファイル。manifest掲載83ファイルとmanifest自身。
- 実装、仕様、報告書、固定データ、全480取得過程の結果、五つの保存セッション／モデル、テスト、検証器、開発記録・比較・計時を同梱。
- `vendor/PLM-L1-v0.12.zip` は無改変。旧資料・コードは入れ子のアーカイブとして保持。
- 結果は `PLM-L1-v0.13/REPORT.md`、実行手順は同フォルダの `README.md`。

ZIP SHA-256：

```text
e4251ee2c510c028f316e1428cb722d5e2eac40d26dfe307b50fd75a647606c6
```

## 実際に行った検証

配布ZIPのSHA-256とCRCを検査し、新規の短い一時ディレクトリへ実展開した。全展開先が一時ルート内であること、アーカイブのルート名、Windowsでの大文字小文字を無視した重複と長さを確認した。その展開物で `verify_release.py --mode functional` を実行した。

- 配布manifest全83ファイルのSHA-256一致。
- ソース凍結、保存結果digest、固定検査の再計算が一致。
- 新規54単体テスト合格。
- 五モデルの隔離再学習：係数差0、厳密指紋一致、機能許容差atol=rtol=1e-12も通過。
- 計764文脈の離散予測と、能動／ランダム教師選択が一致。
- 学習・教師管理モジュール、セッション教師、評価器、正解表を除いた環境で予測と教師選択が動作。
- 別の学習環境で一件だけの教師応答を受け取った更新結果が保存済みhidden-b1と一致。
- 古い要求は終了コード2で拒否し、更新先を作成しない。
- 前版ZIPのCRC・manifest86項目と、引き継いだ位相代数の同一性を確認。

同じ検証器を配布前にstrictモードでも実行し合格した。functionalはモデル／配布物のハッシュ検査を省くモードではない。今回は実測係数差が0なので、機能許容差だけでなく厳密指紋も一致した。

実展開の機械可読記録：`PLM-L1-v0.13-VERIFICATION.json`。実行ログ：`PLM-L1-v0.13-ARCHIVE-VERIFICATION.log`。配布ハッシュ記録：`PLM-L1-v0.13-ARCHIVE.json`。

## 全評価・前版保存

本評価はソース凍結後に二回実行した。480取得過程・2,880 checkpoint・21,128検査が通過し、結果JSONと保存セッション／モデルの計16ファイルがbytes一致した。環境依存の計時は比較対象から除いた。実展開検証で全480過程を三回目として実行したわけではない。

SSの384取得過程と対応するexact対照で、教師の選択順・回答ラベル・候補集合・離散予測が一致した。これはSS固有の優位性の実証ではない。全候補の保持と能動教師選択の効果は、記号的対照でも再現した。

旧版v0.12自身の97テスト（同版40＋継承57）と四モデルの隔離再学習、891文脈×4ポリシー＝3,564判断の一致は配布前に別途確認した。v0.12の295モデル全評価、およびv0.10以前の全テスト・数値評価は今回再実施していない。

従来成果物4,993ファイルのハッシュを確認し変更なし。共有履歴 `PLM-SS-LANGUAGE-DIRECTION.md` は末尾追記だけを別扱いとした。

ソース凍結：

```text
0408962e6e2d56f22e9a223af8bca3f1d5c6cc752116e3fda7f010bf18ca9445
```

結果digest：

```text
0ca67e2d7037b6830ac2908d86c4bea6499d5296fff585ccbceab3d0e2eb5aff
```

## 範囲の注意

Windows 11／Python 3.12.14／NumPy 2.3.5、同一端末内の隔離環境で確認した。Linux実機や異なる機器での成功実績ではない。

教師は人工データの評価側から一件ずつ回答させた。人間への実際の質問・外部教師収集は行っていない。候補・教師・保留の制御は通常処理で、全SS学習ではない。最大3特徴を越える一般的な依存発見、自然文・既存言語系への接続、数値だけの意味不足判定、P1/S1接続は未実施。`eligible_for_inference=false`を維持する。
