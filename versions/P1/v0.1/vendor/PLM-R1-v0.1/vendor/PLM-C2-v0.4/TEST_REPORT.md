# PLM-C2 v0.4 テスト結果

実行 186、合格 186、失敗 0、エラー 0、skip 0。

旧版ごとの凍結実装テストに加え、v0.4の意味役割・個体照応・訂正根拠・校正・R1観察境界を検証。
不正Schema、参照先欠落、元文章との不一致、グラフ破損、主体差し替え、推論モード要求も検査した。

再実行: `python -B verify_release.py`。詳細はTEST_OUTPUT.txt。
